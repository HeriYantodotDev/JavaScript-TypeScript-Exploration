/* eslint-disable @typescript-eslint/no-unused-vars */
function sillyFunction() {
  const list: number[] = [];
  
  // eslint-disable-next-line for-direction
  for (let i=1; i > 0; i ++) {
    list.push(i);
  }
}

// Please uncomment the line below to run the function
// sillyFunction()
